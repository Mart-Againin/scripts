"""
dashboard.py — веб-дашборд для мониторинга всех проектов.

СТАТУС: заглушка. Реализация запланирована.

Планируемый функционал:
- HTTP сервер (FastAPI или aiohttp.web) доступный в браузере
- Страница со статусом всех проектов из launcher.env
- Для каждого проекта:
    - Статус (работает / упал / на паузе)
    - Алертов сегодня
    - Последний алерт (канал, теги, время)
    - Топ каналов по алертам
    - Топ ключей по срабатываниям
    - Тепловая карта активности по часам
    - Динамика по ключу (всплески)
    - Посевная карта
    - Заблокированные теги и время разблокировки
- Автообновление каждые 30 секунд
- Не требует авторизации (только для локальной сети / VPN)

Источники данных:
- results.json каждого проекта (Storage.get_stats_for_dashboard)
- Stats.get_key_dynamics(), get_hourly_heatmap(), get_seed_map()
- launcher.env — список проектов

Запуск (когда будет реализован):
    python dashboard.py
    # открыть http://localhost:8080
"""


def start_dashboard(launcher_env_path: str, host: str = "0.0.0.0", port: int = 8080):
    """
    Заглушка. Будет запускать веб-сервер дашборда.

    Args:
        launcher_env_path: Путь к launcher.env с списком проектов
        host:              Адрес для прослушивания
        port:              Порт
    """
    raise NotImplementedError(
        "Dashboard не реализован. "
        "Планируется в следующей версии."
    )
